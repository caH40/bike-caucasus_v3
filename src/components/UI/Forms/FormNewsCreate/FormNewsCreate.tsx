'use client';

import FormWrapper from '../FormWrapper/FormWrapper';

type Props = {};

export default function FormNewsCreate({}: Props) {
  return <FormWrapper title={'Создание новости'}>FormNewsCreate</FormWrapper>;
}
