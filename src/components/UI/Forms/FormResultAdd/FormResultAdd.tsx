import styles from '../Form.module.css';

type Props = {};

/**
 * Форма добавления результата райдера в финишный Протокол.
 */
export default function FormResultAdd({}: Props) {
  return <form className={styles.wrapper}>FormResultAdd</form>;
}
