import { Icon } from 'leaflet';

// Иконки для Маркеров карты.

export const iconStart = new Icon({
  iconUrl: '/images/icons/start.svg',
  iconSize: [30, 30],
});

export const iconFinish = new Icon({
  iconUrl: '/images/icons/finish.svg',
  iconSize: [30, 30],
});
